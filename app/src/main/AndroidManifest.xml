<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dam_proyecto_final">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DAM_Proyecto_final"
        android:usesCleartextTraffic="true">
        <activity android:name=".registry.RegistryGoogleActivity"></activity>
        <activity android:name=".home.homeui.group_invoice.GroupInvoiceAdd" />
        <activity android:name=".home.homeui.group_invoice.GroupInvoiceTab" />
        <activity android:name=".home.homeui.group_invoice.GroupInvoiceHomeActivity" />
        <activity android:name=".home.homeui.groupui.GroupAddActivity"
            android:windowSoftInputMode="adjustPan"
            android:parentActivityName=".home.HomeActivity"/>
        <activity android:name=".registry.RegistryActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>